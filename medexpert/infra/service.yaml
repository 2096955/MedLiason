apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: medliaison
  labels:
    cloud.googleapis.com/location: us-central1
  annotations:
    run.googleapis.com/launch-stage: BETA
spec:
  template:
    metadata:
      annotations:
        autoscaling.knative.dev/minScale: "1"
        autoscaling.knative.dev/maxScale: "1"
        run.googleapis.com/cpu-throttling: "false"
        run.googleapis.com/startup-cpu-boost: "true"
        run.googleapis.com/container-dependencies: '{"medexpert":["memgraph"]}'
        run.googleapis.com/revision-tag: "deploy-6-feedback-bridge-removed"
    spec:
      containerConcurrency: 80
      timeoutSeconds: 600
      containers:
        # Main container: MedExpert (Redis + 10 MCP + Memgraph MCP client + 13 agents + gateway)
        - name: medexpert
          image: gcr.io/gbg-neuro/medliaison:771fecf7
          ports:
            - name: http1
              containerPort: 8080
          resources:
            limits:
              memory: "12Gi"
              cpu: "6"
          env:
            - name: SOLACE_DEV_MODE
              value: "true"
            - name: NAMESPACE
              value: "medexpert/"
            - name: VERTEXAI_PROJECT
              value: "gbg-neuro"
            - name: VERTEXAI_LOCATION
              value: "us-central1"
            - name: FASTAPI_HOST
              value: "0.0.0.0"
            - name: FASTAPI_PORT
              value: "8080"
            - name: REDIS_URL
              value: "redis://localhost:6379"
            - name: MEMGRAPH_URL
              value: "bolt://localhost:7687"
            - name: MEMGRAPH_USER
              value: "memgraph"
            - name: MEMGRAPH_PASSWORD
              value: "medexpert-demo"
            - name: SESSION_SECRET_KEY
              value: "medliaison-demo-session-key"
          startupProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 60
            periodSeconds: 15
            failureThreshold: 40
            timeoutSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            periodSeconds: 30
            timeoutSeconds: 10

        # Sidecar: Memgraph graph database (no ports — only ingress container exposes ports)
        # Accessible from main container via bolt://localhost:7687
        - name: memgraph
          image: memgraph/memgraph:latest
          args: ["--also-log-to-stderr", "--log-level=WARNING"]
          resources:
            limits:
              memory: "4Gi"
              cpu: "2"
          env:
            - name: MEMGRAPH_USER
              value: "memgraph"
            - name: MEMGRAPH_PASSWORD
              value: "medexpert-demo"
          startupProbe:
            tcpSocket:
              port: 7687
            initialDelaySeconds: 5
            periodSeconds: 5
            failureThreshold: 20
            timeoutSeconds: 3
